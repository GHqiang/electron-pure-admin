# wanda V3 L0 报价/出票字段上送采集（2026-08-19）

> 关联方案：V3 日志整改（见《2026-08-18-chenxing-v3字段采集示范.md》），本文件为 **wanda（万达）系列推广**
> 范围：**纯增量字段采集**，严格照 jinyi 已完成样板；不改任何业务逻辑、不动公共基类（BaseOfferQueue/BaseOfferPrice/BaseTicketQueue 已生效）

## 一、修改文件清单

| 文件 | 改动 |
|------|------|
| `src/common/autoTicket/buyTicket/wanda/offerManage.js` | calc_path 过程拼装（初始匹配/电影格式/会员日/券库存/会员价加价/定价策略段）+ hit_rule_name 挂载 |
| `src/common/autoTicket/buyTicket/wanda/buyTicket.js` | 出票字段采集（paymentAmount/cardBalance/coupons/usePath）+ return 挂载 + buildTicketUsePath 方法 |

不动：chenxing、公共文件（BaseOfferQueue/BaseOfferPrice/BaseTicketQueue/platManage）。
未动：`wanda/cardQuanManage.js`、`wanda/orderManage.js`（工作区既有改动，日志文案类，与本任务无关，属此前打点整改遗留）。

## 二、报价侧（offerManage.js）

### calcSteps 拼装段（getEndMatchOfferRule + getMinAmountOfferRule）

```
初始匹配命中N条
→ 电影格式过滤剩N条            （filterByFilmType(movieInfo.media) 后）
→ 会员日规则命中(X元)           （filterMemberDayRules 命中即返回）
→ 券库存过滤剩N条               （validateQuanStock 库存+当日票数过滤后）
→ 会员价加价命中(X元)           （processAddRule 后 memberOfferAmount）
→ 定价策略段                     （仅固定价 / 仅会员价 / 固定 vs 会员取低，对照 chenxing 模式）
```

- `getMinAmountOfferRule(ruleList, order, movieInfo, calcSteps = [])` 签名加 `calcSteps = []`（默认值兼容其它调用点）
- 返回前：`const endRuleCopy = JSON.parse(JSON.stringify(endRule)); endRuleCopy.calc_path = calcSteps.join("→"); endRuleCopy.hit_rule_name = endRule.ruleName || "";`
- `calc_path`/`hit_rule_name` 由 BaseOfferQueue serOrderInfo 收口（截断 500）入库，本文件只负责挂到 offerRule

## 三、出票侧（buyTicket.js）

### 采集字段（oneClickBuyTicket 成功路径 return 前）

| 字段 | 来源 | 单位 |
|------|------|------|
| `paymentAmount` | 本地计算 `orderPrice - couponDeduction - cardPayPrice`（第 466 行既有变量，orderPrice 已 /100） | **已是元**，`?? null` 原样采集 |
| `cardBalance` | `if (card_id)` 块内既有 `oldBalance = (canUseCardList?.find(cardNo==card_id)?.balance \|\| 0) / 100` | **已是元**（/100 转元），`?? null` 采集 |
| `coupons` | `(useQuan \|\| []).slice(0, 10).map({couponType, couponCode, couponName})` | 快照数组 |
| `usePath` | `buildTicketUsePath({offer_type, useQuan, cardNum, cardBalance, paymentAmount, currentParamsInx})` | 一句话路径 |

### 作用域处理

- `cardBalance` 本文件两处声明均在块内（第 461 行 `if (offer_type !== "1")` 块内 `const cardBalance`、第 704 行 `if (card_id)` 块内 `const oldBalance`），return 处均不可访问 → **照 jinyi 模式在 `if (card_id)` 前提升 `let cardBalance = null;`**，块内 `oldBalance` 计算后追加 `cardBalance = oldBalance;`（支付前余额，元）
- 第 461 行块内 `const cardBalance` 为既有遮蔽声明，未动（外层提升后形成合法遮蔽，eslint no-shadow 未启用，无告警）
- `cardNum`/`card_id` 声明于第 575 行（函数级作用域），return 处可直接访问，无需提升

### buildTicketUsePath 方法

方法体照抄 jinyi（换号N次 / 用券N张→卡尾号付券手续费→实付 / 用卡尾号→支付前余额→实付），保持 wanda 文件风格。

## 四、验证结果

- [x] `node --check`：两文件均通过（offerManage.js / buyTicket.js syntax OK，exit 0）
- [x] eslint：`npx eslint wanda/offerManage.js wanda/buyTicket.js`
  - offerManage.js：**0 error**
  - buyTicket.js：仅 **1 个存量 prettier error**（164:28，`let targetSeatRes =` 折叠行，**不在本次改动行内**）；基线（改动前）为 3 个存量 error（164/174/303），其中 174/303 为混合行尾 CRLF 类错误，编辑工具整写文件时顺带归一为 CRLF（git autocrlf 归一化下 diff 不可见、无噪音）
- [x] git diff 复查：仅字段采集相关行，无格式化噪音（buyTicket.js +60/-1、offerManage.js +30/-3，均对应采集代码）；工作区另两个改动文件（cardQuanManage/orderManage）为既有改动非本次产出

## 五、回归风险评估

| 风险 | 等级 | 控制 |
|------|------|------|
| 数值列 `""` 写 DECIMAL 报 1366 | 低 | return 处 `paymentAmount ?? null` / `cardBalance ?? null` 兜底（与基类收口一致，双层防呆） |
| 提前 return 路径（转单/换号/测试模式）不采集 | 低 | saveTicketRecord 解构默认值兜底（undefined→NULL/空），行为与原一致 |
| calcSteps 拼装引用不存在字段 | 低 | 全部 `?.` / 既有变量保护；失败路径（endRule 为空/黑名单/售罄）不挂 calc_path |
| 提升 cardBalance 引入遮蔽 | 低 | 仅嵌套块遮蔽，同作用域无重声明，eslint 无规则触发 |

## 六、说明（结构差异与处理）

- wanda getEndMatchOfferRule 有"黑名单标签先不报价/已售罄先不报价"前置判断（dictStore 黑名单、seatInfo.soldOut），**未动**，仅在正常报价路径收集 calcSteps
- wanda getMinAmountOfferRule 内部还有一次 fixedRules 的电影格式二次过滤（第 364-376 行）——与 jinyi 结构一致（jinyi 同样存在但未单独 push），照 jinyi 样板不为其单独 push，`电影格式过滤剩N条` 已在 getEndMatchOfferRule 层收集一次
- `paymentAmount` 无需新增：既有本地计算（第 466 行）即为元（orderPrice 元 - 券抵扣元 - 卡付元），直接采集
- 未提交，待用户指令
