---
description: 仅格式化本次修改过的文件，不整库格式化
alwaysApply: true
---

# 格式化范围

- **仅格式化本次修改过的文件**：每次代码调整后，只对本次实际编辑/改动的文件执行格式化（如 Prettier、ESLint --fix 等）。
- **不要整库格式化**：不要对未改动的文件执行格式化，不要对整个项目、整个目录或大量未修改文件运行 format 命令。
- **命令示例**：若需格式化，只针对改动文件执行，例如 `npx prettier --write path/to/changed-file.js`，不要使用 `**/*.js` 或 `.` 等全量路径。
